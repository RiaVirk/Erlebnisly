shadcn’s <Calendar> is date-only (uses react-day-picker). You will build a custom <TimeSlotPicker> component in Phase 1/2 — note this in your README or a TODO.md.
curl -H "Authorization: Bearer your-cron-secret" http://localhost:3000/api/cron/expire-holds
