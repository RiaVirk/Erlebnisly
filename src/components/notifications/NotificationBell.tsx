"use client";

// In-app notification bell with unread count badge.
// Implementation added in notification step.
export function NotificationBell() {
  return <button aria-label="Benachrichtigungen" />;
}
