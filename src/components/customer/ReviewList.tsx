// Displays paginated reviews for an experience.
// Implementation added in review step.
export function ReviewList({ experienceId }: { experienceId: string }) {
  return <div data-experience-id={experienceId} />;
}
