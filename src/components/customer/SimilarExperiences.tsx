// Similar experiences grid shown below the main experience detail.
// Implementation added in search/discovery step.
export function SimilarExperiences({ experienceId }: { experienceId: string }) {
  return <div data-experience-id={experienceId} />;
}
