// Paginated bookings table for the host bookings page.
// Implementation added in host bookings step.
export function BookingsTable() {
  return <div className="rounded-lg border p-4 text-muted-foreground">Tabelle wird geladen…</div>;
}
