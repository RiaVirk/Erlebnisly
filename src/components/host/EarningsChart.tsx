"use client";

// Recharts line/bar chart for host earnings over time.
// Install recharts, then implement. Implementation added in earnings step.
export function EarningsChart() {
  return <div className="h-64 rounded-lg border flex items-center justify-center text-muted-foreground">Diagramm wird geladen…</div>;
}
